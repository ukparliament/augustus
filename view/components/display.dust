{! This is a partial not a component, it must be used in conjunction with a component that can take it as an option.  !}
class="
  {#data.display.data}
    {?component}
      {component}
    {/component}
    {?variant}
      --{variant}
    {/variant}
    {~s}
  {/data.display.data}
"
