language: node_js
node_js:
  - 6
  - 8
  - node

matrix:
  allow_failures:
  - node_js: node

before_install:
  - npm install -g codeclimate-test-reporter

script:
  - npm test
  - codeclimate-test-reporter < ./coverage/lcov.info